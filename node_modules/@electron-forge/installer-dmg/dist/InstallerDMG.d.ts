import InstallerDarwin, { InstallerOptions } from '@electron-forge/installer-darwin';
export default class InstallerDMG extends InstallerDarwin {
    name: string;
    install({ filePath, installSpinner, }: InstallerOptions): Promise<void>;
}
