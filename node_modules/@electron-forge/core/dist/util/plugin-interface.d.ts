import { IForgePluginInterface, ForgeConfig } from '@electron-forge/shared-types';
import { StartOptions } from '../api';
export default class PluginInterface implements IForgePluginInterface {
    private plugins;
    private config;
    constructor(dir: string, forgeConfig: ForgeConfig);
    triggerHook(hookName: string, hookArgs: any[]): Promise<void>;
    triggerMutatingHook<T>(hookName: string, item: T): Promise<T>;
    overrideStartLogic(opts: StartOptions): Promise<string | false | string[] | import("@electron-forge/shared-types").ElectronProcess>;
}
