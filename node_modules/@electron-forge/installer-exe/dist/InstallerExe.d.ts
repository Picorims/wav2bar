import InstallerBase, { InstallerOptions } from '@electron-forge/installer-base';
export default class InstallerExe extends InstallerBase {
    name: string;
    install({ filePath, }: InstallerOptions): Promise<void>;
}
